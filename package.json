{
  "private": true,
  "scripts": {
    "build": "pulp build --to dist/app.js",
    "build-example": "pulp browserify --include examples/src --optimise --to examples/dist/example.js",
    "watch": "pulp -w build --to dist/app.js",
    "watch-fast": "webpack --entry ./entry.js --output-filename dist/app.js --progress --watch",
    "watch-example": "pulp -w browserify --include examples/src --optimise --to examples/dist/example.js"
  },
  "devDependencies": {
    "pulp": "^12.0.1",
    "purescript-psa": "^0.5.1",
    "webpack": "^3.6.0"
  }
}
