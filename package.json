{
  "private": true,
  "scripts": {
    "build": "pulp build --to html/app.js",
    "watch": "pulp -w build --to html/app.js",
    "watch-fast": "webpack --entry ./entry.js --output-filename html/app.js --progress --watch"
  },
  "devDependencies": {
    "pulp": "^12.0.1",
    "purescript-psa": "^0.5.1",
    "webpack": "^3.6.0"
  }
}
